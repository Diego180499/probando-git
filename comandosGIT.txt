** git init  - iniciar un entorno para git

** git add (nombre de archivos)  - mandamos al staged los archivos que querramos

** git commit -m "mensaje"  -  hacemos un mensaje para comprometer.

** git rm <archivo>     -  para eliminar un archivo

** git restore --staged <archivo>..." para sacar del área de stage)

** git restore <archivo>... para descartar los cambios en el directorio de trabajo

** git status   -   para ver el estado del stage

** el archivo .gitignore es para ignorar todos los archivos
que se encuentren dentro de el.

** git status -s    -    para ver que archivos han sido modificados,
que archivos necesitan ser agregados, para ver el estado general de
cada archivo.

___modificaciones estando en rama b_____
** git branch  -  para ver en que rama estamos posicionados

** git checkout -b <nombre rama>   -   para agregar una rama y
 posicionarnos en ella.

** git checkout <nombre rama>   -   para posicionarnos en la rama.








